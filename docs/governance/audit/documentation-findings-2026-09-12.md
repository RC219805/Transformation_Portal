# Documentation Finding Ledger — September 12, 2026

Source baseline: `faa3758fc3b5180bccf264c9a1b1d1b5ef927c52`.
“Fixed” describes the bounded local patch; it does not mean published, deployed,
or exercised with live models/services. See the [validation ledger](documentation-validation-2026-09-12.md).

| Finding / status | Current evidence and affected files | User-visible consequence | Bounded change and acceptance criteria | Missing input / limit |
| --- | --- | --- | --- | --- |
| Machine parser top-level errors — fixed | `tools/parse_machine_json.py`; producer `ingest/machine_output.py`; subprocess regressions | Valid failure envelopes formerly exited 99 | Route non-null top-level errors first; preserve envelope exit code; stderr diagnostics; exercise typed/generic, empty/producer data, stdin/file, success/domain errors and malformed input | No schema or serializer change; no live extraction claim |
| ComfyUI graph reload — fixed | `comfyui/workflow_builder.py`, `executor.py`; workflow-builder/template tests | Edges were lost although JSON equality passed | Save explicit edges; restore relationships, slots, metadata and ordinary lists; assert dependency order and resolved inputs for connected/branching/reordered graphs | Legacy link-shaped literal lists are ambiguous without explicit edge metadata |
| ComfyUI execution handlers — deferred | `comfyui/executor.py` dispatch and unsupported-node guard; `custom_nodes.py` output contracts | A generated template can fail closed for INPUT/OUTPUT/FLUX and other unsupported nodes | Separate handler-by-handler proposal with bounded input/output contracts and model-free dispatch tests before live inference; align SkyGAN mask/report output contracts | Executor owner decision, optional dependencies/models and output contract scope; not an execution-engine implementation in this refresh |
| Editorial installation closure — deferred | `tools/ad_editorial_post_pipeline.py`; `scripts/README.md`; same-process RAW/ReportLab imports | Lux isolated RAW installation cannot satisfy this editorial process | Define a governed editorial dependency lane, supported host/toolchain and reproducible install; verify RAW ingest and PDF report in that interpreter | Dependency-policy approval; representative RAW inputs; ReportLab/RAW dependency compatibility |
| Legacy nightly benchmark evidence — deferred | `.github/workflows/nightly.yml`; `scripts/ci/check_performance_evidence.py`; performance docs | Legacy timings/baseline handling and old ML install pins are not the repaired Performance Monitor evidence contract | Separate nightly update to governed dependencies and explicit writer/artifact/committed-baseline validation; fail on missing/stale evidence and test first-run/regression behavior | CI/dependency owner decision and real workload baseline; accepted gate policy unchanged |
| Model installer diagnostic advice — fixed, runtime behavior deferred | `scripts/setup/install_models.py`; `scripts/verification/verify_ml_deps.py` and focused diagnostics tests | Ad hoc main-environment installs and import-only “ready for APEX” messages misled operators | Point to supported isolated/governed setup; explicitly limit successful checks to current-process imports; assert diagnostic output | Model utility installation/download behavior remains separate policy work |
| DA3 cache authority — blocked for live verification | target-owned DA3 lock/installer; Lux execution identity and lifecycle; `requirements/README.md` | Inference capability alone cannot authorize identity-v3 cache access | Validate native Darwin arm64/Python 3.11 baseline, trusted runtime/model/materialized input identities, then exercise miss/write/hit and invalidation with governed evidence | Trusted installed runtime/checkpoint and approved real inputs; no authority markers fabricated |
| FastVLM authority/inference — blocked for live verification | `scripts/validation/validate_fastvlm_runtime.py`; `docs/runtimes/fastvlm.md` | Manifest presence alone does not authorize runtime or prove captions | Verify the v2 builder/dependency/source/model contract, distinguish static verification from import/MLX-Metal smoke and real caption artifacts | Trusted original builder, model weights and target Metal session; no downloads or fabricated manifests |
| Tenant identity and managed services — blocked for live verification | `app.py::_pilot_tenant_from_request`; orchestrator queue/storage/artifact-store modules; deployment runbooks | Header-based pilot tenant plumbing and local contracts do not prove provider identity binding or multi-host readiness | Bind authenticated actor to tenant and reject spoofed cross-tenant requests; rerun managed Postgres/Redis/S3 gate, worker reconnect/reclaim and artifact authorization with exact deployment evidence | Provider endpoints, credentials, deployment owner and authenticated identity policy |
| Postgres guide — fixed | `orchestrator/storage/postgres.py`, migrations, Makefile, runtime guide | Fictional tuning and optimistic retry guidance hid row-lock/migration prerequisites | Document actual `FOR UPDATE` behavior, migrations and API key; remove unsupported pool environment variable | Live concurrent database test not run |
| CLI and public startup — fixed | Lux parser/lifecycle/V2 runner; Makefile; `app.py`; CLI/API/public guides | Invalid boolean/apex examples and nonexistent `cli serve` could fail; saved Build drafts could remain stale | Align explicit optional flags/preset precedence/V2 disable; maintained Make/Uvicorn launcher and cross-terminal env ordering; distinguish plan/readiness/inference/artifacts | No live inference or browser deployment claim |
| Architectural PDF/rendering — fixed documentation, integration deferred | `scripts/analysis/architectural_context_extractor.py`; `archive/scripts/`; context guide | Extracted text could be mistaken for OCR, verified dimensions or automatic rendering integration | Document maintained extraction separately; require a separate tested integration design before promotion | Representative drawings, OCR/dimension validation scope, integration owner |
| Scheduled security audit / branch protection — fixed documentation | `SECURITY.md`, `CONTRIBUTING.md`; read-only GitHub protection response dated September 12 | Past schedules and settings were presented as current certification | Preserve dated snapshots, require completion evidence for audit claims and live readback for enforcement claims | No independent audit report established; remote settings may change |
| Documentation inventory — fixed | Full tracked-document inventory and existing TODO/design generators | Inventory size could be confused with complete source review | Explicit per-file review statuses; generated snapshots refreshed only through existing tools; historical entries preserved | Inventory-only files remain unverified at paragraph level |
| Heading-anchor punctuation — fixed | `scripts/validation/check_doc_heading_links.py`; existing ADR-049 link and focused validator tests | A valid GitHub anchor was incorrectly rejected because the checker retained periods | Remove periods from derived anchors; test punctuated headings and duplicate normalized anchors while retaining the valid original link | Scanner remains a bounded Markdown checker, not a complete GitHub renderer |
| Five original link targets — deferred | APEX executive summary (two links), Tranche Phase 2 summary, performance-ledger ADR-023, PR934 extraction strategy, additional APEX research ADR-019 summary | Historical navigation has unresolved references | Recover original content/identity from an authoritative source; repair only then; see link backlog | Original target evidence unavailable; similarly named documents rejected |
