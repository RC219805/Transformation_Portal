# Minimal dependencies for CI linting jobs
# These are only the packages needed for linting tools/ and root *.py files
# Does NOT include heavy ML dependencies (torch, transformers, diffusers, realesrgan, etc.)

# Core dependencies that are actually imported in linted files
numpy>=1.24,<2.5.0
Pillow>=10.3.0,<13  # Security: CVE-2024-28219 fixed in 10.3.0; current governed lock baseline is pillow==12.3.0
PyYAML>=6.0,<7
tqdm>=4.66,<5

# Optional dependencies for specific tools
typer>=0.12,<1  # CLI framework
reportlab>=4.5.0,<5  # PDF generation (used in some tools)
rawpy>=0.18.0,<1  # RAW image processing (used in some tools)

# Testing framework (needed for linting test files in tools/)
pytest>=8,<10

# Linting tools (exact pins for CI/local parity)
# Keep these in sync with pre-commit hook version checks.
flake8==7.3.0
black==26.3.1
isort==8.0.1
pylint==4.0.6
