# Dev environment: runtime + testing + tooling
-r requirements-ci.txt

# Type stubs
types-PyYAML>=6.0.12

# Property-based testing (v1.7 requirement)
hypothesis>=6.100,<7

# Flaky-test quarantine support (ADR-033)
pytest-rerunfailures>=14.0,<17

# JSON Schema validation (machine-mode contract tests)
jsonschema>=4.21.0,<5

# Required for FastAPI/Starlette TestClient in HTTP contract tests
httpx>=0.28,<1

# Optional Phase 3.1 detached signing support (tools + tests)
cryptography>=48.0.1,<51

# S3 mock for the Phase 4.A ArtifactStore contract tests
moto[s3]>=5.0,<6

# Linters, formatters, type checkers
mypy>=1.10
flake8>=7
pylint>=3
black>=26.3.1
isort>=5.13,<9
autopep8>=2.0,<3
libcst>=1.2,<2
pre-commit>=4.0,<5

# Documentation generation
sphinx>=7.2,<9
sphinx-rtd-theme>=2.0,<3
sphinx-autodoc-typehints>=2.0,<4
