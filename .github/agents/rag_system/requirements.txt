# RAG System Requirements
# Core dependencies for the Retrieval-Augmented Generation system

# Core dependencies
numpy>=1.24.0,<3
scikit-learn>=1.3.0  # For TF-IDF and BM25 implementation
PyYAML>=6.0  # For configuration management

# Optional semantic/vector search is not supplied by this file or by [ml].
# No governed semantic-search runtime/lock exists. Keep offline BM25 enabled;
# propose a separate target-specific dependency/model contract before installation.
