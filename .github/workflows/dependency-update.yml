# .github/workflows/dependency-update.yml
# Phase 2: Automated Dependency Management
name: Dependency Updates

on:
  schedule:
    # Run weekly on Mondays at 9 AM UTC
    - cron: '0 9 * * 1'
  workflow_dispatch:  # Manual trigger

permissions:
  contents: write
  pull-requests: write

jobs:
  update-dependencies:
    name: Update Python Dependencies
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        id: setup-python
        with:
          python-version: "3.11"

      - name: Install lock generation tools
        shell: /usr/bin/env -u BASH_ENV -u ENV /bin/bash --noprofile --norc -p -e -o pipefail {0}
        run: |
          "${{ steps.setup-python.outputs.python-path }}" -I -m pip --isolated install --upgrade "pip==26.2.1"
          "${{ steps.setup-python.outputs.python-path }}" -I -m pip --isolated install "pip-tools==7.6.1" "click==8.4.2"
          # Install pip-audit from governed security lockfile
          "${{ steps.setup-python.outputs.python-path }}" -I -m pip --isolated install -r requirements/security.txt

      - name: Preflight dependency update targets
        shell: /usr/bin/env -u BASH_ENV -u ENV /bin/bash --noprofile --norc -p -e -o pipefail {0}
        run: |
          set -euo pipefail
          "${{ steps.setup-python.outputs.python-path }}" -I scripts/validation/check_dependabot_config.py
          "${{ steps.setup-python.outputs.python-path }}" -I scripts/validation/check_worker_dependency_parity.py
          "${{ steps.setup-python.outputs.python-path }}" -I scripts/validation/check_lock_ownership.py \
            --context ubuntu-x64-generic

      # Free up disk space before dependency generation and auditing.
      - name: Free disk space
        shell: /usr/bin/env -u BASH_ENV -u ENV /bin/bash --noprofile --norc -p -e -o pipefail {0}
        run: |
          echo "Disk space before cleanup:"
          df -h

          # Remove large unnecessary tools and libraries
          sudo rm -rf /usr/share/dotnet || true
          sudo rm -rf /opt/ghc || true
          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf /opt/hostedtoolcache/CodeQL || true
          sudo rm -rf /usr/local/share/boost || true

          # Clean Docker
          sudo docker image prune --all --force || true
          sudo docker container prune --force || true

          # Clean apt cache
          sudo apt-get clean || true

          echo "Disk space after cleanup:"
          df -h

      - name: Update dependencies
        shell: /usr/bin/env -u BASH_ENV -u ENV /bin/bash --noprofile --norc -p -e -o pipefail {0}
        env:
          # pip 26.2 caches Simple API responses. Disabling that cache keeps
          # newly published releases visible to this scheduled update lane.
          PIP_NO_CACHE_DIR: "1"
        run: |
          set -euo pipefail

          if [ ! -d "requirements" ]; then
            echo "requirements/ directory not found"
            exit 1
          fi

          echo "Updating generic governed requirements with pip-compile..."
          cd requirements
          make update-generic LOCK_PYTHON_VERSION=3.11
          make check-generic LOCK_PYTHON_VERSION=3.11
          cd ..

      - name: Verify lockfile contract
        shell: /usr/bin/env -u BASH_ENV -u ENV /bin/bash --noprofile --norc -p -e -o pipefail {0}
        run: |
          "${{ steps.setup-python.outputs.python-path }}" -I scripts/validation/check_requirements_lock_contract.py

      - name: Check for vulnerabilities
        shell: /usr/bin/env -u BASH_ENV -u ENV /bin/bash --noprofile --norc -p -e -o pipefail {0}
        run: |
          set -euo pipefail

          audit_reports_dir="${{ runner.temp }}/dependency-update-audit-reports"
          rm -rf "${audit_reports_dir}"
          mkdir -p "${audit_reports_dir}"
          audit_targets=(
            requirements/all.txt
            requirements/base.txt
            requirements/dev.txt
            requirements/ci.txt
            requirements/security.txt
            requirements/tools-archive.txt
          )

          for requirement_file in "${audit_targets[@]}"; do
            report_path="${audit_reports_dir}/$(basename "${requirement_file%.txt}").json"
            echo "Auditing ${requirement_file} -> ${report_path}"
            pip-audit \
              -r "${requirement_file}" \
              --format json \
              --output "${report_path}" || true
          done

      - name: Upload pip-audit report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7
        with:
          name: pip-audit-report
          path: ${{ runner.temp }}/dependency-update-audit-reports/
          if-no-files-found: warn
          retention-days: 30

      - name: Check lock ownership authority
        shell: /usr/bin/env -u BASH_ENV -u ENV /bin/bash --noprofile --norc -p -e -o pipefail {0}
        run: |
          set -euo pipefail

          baseline_sha="${{ github.sha }}"
          current_sha="$(/usr/bin/git rev-parse --verify 'HEAD^{commit}')"
          if [ "${current_sha}" != "${baseline_sha}" ]; then
            echo "ERROR: dependency automation must not create or amend commits before publication."
            exit 1
          fi

          changed_files_path="$(mktemp)"
          trap 'rm -f "${changed_files_path}"' EXIT

          /usr/bin/git diff --name-only "${baseline_sha}" > "${changed_files_path}"
          /usr/bin/git ls-files --others --exclude-standard >> "${changed_files_path}"
          LC_ALL=C sort -u -o "${changed_files_path}" "${changed_files_path}"

          "${{ steps.setup-python.outputs.python-path }}" -I scripts/validation/check_lock_ownership.py \
            --context ubuntu-x64-generic \
            --changed-files-file "${changed_files_path}"

          current_sha="$(/usr/bin/git rev-parse --verify 'HEAD^{commit}')"
          if [ "${current_sha}" != "${baseline_sha}" ]; then
            echo "ERROR: dependency automation must not create or amend commits during publication validation."
            exit 1
          fi

          /usr/bin/git diff --name-only "${baseline_sha}" > "${changed_files_path}"
          /usr/bin/git ls-files --others --exclude-standard >> "${changed_files_path}"
          LC_ALL=C sort -u -o "${changed_files_path}" "${changed_files_path}"

          if grep -Eq '^requirements/ml-core-darwin-arm64.txt$' "${changed_files_path}"; then
            echo "ERROR: scheduled dependency automation must not modify target-owned ML locks."
            exit 1
          fi

          if grep -Eqv '^requirements/(all|base|ci|dev|security|tools-archive)\.txt$' \
            "${changed_files_path}"; then
            echo "ERROR: scheduled dependency automation produced unexpected requirements changes:"
            grep -Ev '^requirements/(all|base|ci|dev|security|tools-archive)\.txt$' \
              "${changed_files_path}"
            exit 1
          fi

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1  # v8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore: update dependencies (automated)"
          title: "🔄 Automated Dependency Updates"
          body: |
            ## 📦 Dependency Updates

            This PR contains automated dependency updates using the layered requirements system.

            ### 📁 Updated Files
            - `requirements/base.txt` - Core runtime dependencies
            - `requirements/dev.txt` - Development tools
            - `requirements/ci.txt` - CI/CD tools
            - `requirements/security.txt` - Governed security scanning toolchain
            - `requirements/tools-archive.txt` - Archive reporting toolchain
            - `requirements/all.txt` - Combined requirements

            ### 🎯 ML Layer Strategy
            scheduled automation updates generic locks only
            - `ml-core-darwin-arm64.txt` remains a manual Apple Silicon authoritative lane
            - retired Linux/macOS Intel ML lanes are not checked-in installable requirements
            - **ml-sam2.in** remains scripted-only and is not a standard checked-in lockfile

            ### 🔍 Security Report
            View the `pip-audit-report` artifact in the workflow run for vulnerability scan results.

            ### ⚠️ Review Required
            - [ ] Check for breaking changes
            - [ ] Review security report
            - [ ] Validate tests pass
            - [ ] Verify compatibility
            - [ ] Confirm target-owned ML lock contracts still match ADR-032 and `requirements/lock_ownership.yml`

            **Generated automatically by GitHub Actions**
          branch: automated/dependency-updates
          add-paths: |
            requirements/all.txt
            requirements/base.txt
            requirements/dev.txt
            requirements/ci.txt
            requirements/security.txt
            requirements/tools-archive.txt
          delete-branch: true
          labels: |
            dependencies
            automated
