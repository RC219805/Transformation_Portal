# -----------------------------------------------------------------------------
# Python
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
# Explicit .pyc required by structural test
*.pyc
*$py.class
*.so
.Python

# Build / packaging
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
!scripts/lib/
!scripts/lib/*.py
!web/secure-landing/lib/
!web/secure-landing/lib/**

# Caches
.hypothesis/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pyre/
requirements/.pip-tools-cache/
requirements/.hash-pilot/

# -----------------------------------------------------------------------------
# IDEs / editors
# -----------------------------------------------------------------------------
.idea/
.vscode/
.ipynb_checkpoints/

# pyenv
.python-version

# -----------------------------------------------------------------------------
# Environments
# -----------------------------------------------------------------------------
.env
.venv/
.venv-da3/
.venv-raw/
.venv-lint/
# Covers optional local runtimes such as FastVLM checkpoints, proxies,
# benchmark reports, and patched vendor checkouts under .runtime/fastvlm/.
.runtime/
env/
venv/
path/to/venv
ENV/
env.bak/
venv.bak/
venv_py311/

# -----------------------------------------------------------------------------
# Security - sensitive files
# -----------------------------------------------------------------------------
.bash_history
.zsh_history
.sh_history
*.pem
*.key
id_rsa
id_dsa
PKG-INFO

# -----------------------------------------------------------------------------
# Logs / temp / patches
# -----------------------------------------------------------------------------
*.log
*.tmp
*.temp
*.backup
*.patch
*.diff

# -----------------------------------------------------------------------------
# macOS
# -----------------------------------------------------------------------------
.DS_Store
._*

# -----------------------------------------------------------------------------
# Tools / binaries (install via CI/package managers)
# -----------------------------------------------------------------------------
actionlint
*.exe

# -----------------------------------------------------------------------------
# Test outputs / build outputs
# -----------------------------------------------------------------------------
processed_images/
output/
output_*/
test_output/
test_artifacts/
extracted_context/
docs/api/_build/
web/secure-landing/.next/
web/secure-landing/.next-build-verify/
web/secure-landing/.next-smoke-*/
web/secure-landing/.next-codex-*/
web/secure-landing/node_modules/
cloudflare/transformationportal-worker/.wrangler/
cloudflare/transformationportal-worker/node_modules/

# Coverage artifacts
.coverage
coverage.json
htmlcov/
.coverage.*

# Spatial AI benchmark telemetry (mutable local test artifact)
tests/spatial_ai/performance_ledger.json

# -----------------------------------------------------------------------------
# Depth Pipeline
# -----------------------------------------------------------------------------
# Model cache
.cache/

# Depth maps and outputs
*_depth.npy
*_depth_viz.png
*_enhanced.png
*_enhanced.jpg
processed_output/
depth_maps_apex/

# Local RAW test inputs (never commit)
input_images/Richard-Raw-Test/

# Model weights and checkpoints
*.pth
*.safetensors
*.ckpt
*.bin
*.pt
*.pb
*.onnx
*.tflite
*.mlmodel
*.mlpackage

# Model weights/checkpoints stored at repo root (not Python packages)
#/models is a data dir; allow src/**/models as python packages below
/models/
/weights/
/checkpoints/
/pretrained/

# Allow Python packages named 'models'
!src/**/models/__init__.py
!src/**/models/*.py

# Real-ESRGAN specific
RealESRGAN_*.pth
realesr-*.pth

# -----------------------------------------------------------------------------
# Generated reports and artifacts
# -----------------------------------------------------------------------------
*_report.json
*-report.json
material_response_report.json
*_summary.json

# Keep quality/metadata/comparison JSON even if matched above
!*quality*.json
!*metadata*.json
!*comparison*.json
!depth_model_comparison.json

# -----------------------------------------------------------------------------
# Large image files (exclude binary outputs)
# -----------------------------------------------------------------------------
*.tif
*.tiff

# Allow test fixtures (explicit)
!tests/fixtures/pipelines/**/*.tif
!tests/fixtures/pipelines/**/*.tiff

# -----------------------------------------------------------------------------
# Sample / input images (local development only)
# Keep directory structure; ignore image contents
# -----------------------------------------------------------------------------
data/sample_images/**/*.jpg
data/sample_images/**/*.jpeg
data/sample_images/**/*.png
data/sample_images/**/*.tif
data/sample_images/**/*.tiff
!data/sample_images/.gitkeep

input_images/**/*.jpg
input_images/**/*.jpeg
input_images/**/*.png
input_images/**/*.tif
input_images/**/*.tiff
input_images/**/*.[3][fF][rR]
input_images/**/*.[aA][rR][wW]
input_images/**/*.[cC][rR][2]
input_images/**/*.[cC][rR][wW]
input_images/**/*.[dD][nN][gG]
input_images/**/*.[iI][iI][qQ]
input_images/**/*.[nN][eE][fF]
input_images/**/*.[nN][rR][wW]
input_images/**/*.[oO][rR][fF]
input_images/**/*.[pP][eE][fF]
input_images/**/*.[rR][aA][fF]
input_images/**/*.[rR][wW][2]
input_images/**/*.[sS][rR][2]
input_images/**/*.[sS][rR][fF]
!input_images/.gitkeep

# -----------------------------------------------------------------------------
# RAG system generated files
# -----------------------------------------------------------------------------
.rag_cache/

# -----------------------------------------------------------------------------
# Local backup directories (never commit)
# -----------------------------------------------------------------------------
.local_backup/
.backup_local/
.branch_cleanup_backup/

# -----------------------------------------------------------------------------
# Generated logs and artifacts (generic)
# -----------------------------------------------------------------------------
logs/
artifacts/
trial_runs/

# -----------------------------------------------------------------------------
# Temporary markdown files (keep permanent docs only)
# -----------------------------------------------------------------------------
*_TEMP.md
*_WIP.md
SESSION_*.md
Analysis_Complete.md
IMPLEMENTATION_COMPLETE.md
*_SUMMARY.md
COMMIT_SUMMARY_OLD.md

# Keep production documentation
!750_PICACHO_*.md
!CRITICAL_*.md
!Root_Cause_Analysis.md
!Corrective_Action_Plan.md
!Immediate_Recommendations.md
!QA_Improvements.md
!DEPTH_MODEL_*.md
!LUXURY_ESTATE_*.md
!ELITE_PIPELINE_*.md
!PIPELINE_*.md
!QUALITY_*.md
!PHASE1_*.md
!PHASE2_*.md
!Visual_Feedback_Analysis_Summary.md
!COMMIT_SUMMARY.md

# But allow important docs in docs/
!docs/guides/CODE_QUALITY_SYSTEM.md

# -----------------------------------------------------------------------------
# Workflow markers (operational artifacts)
# -----------------------------------------------------------------------------
DEFER
MERGE
ROLLBACK
SUCCESS
Continue
SKIP
INVESTIGATE
=
.empty-commit-trigger
.pr-branch-marker
.pr-marker-*
.events/

# -----------------------------------------------------------------------------
# PDF ingest / extraction artifacts (generated)
# -----------------------------------------------------------------------------
artifact_store/
picacho_architectural/
picacho_construction/
*.xlsx
*.xls

# -----------------------------------------------------------------------------
# Organized outputs directory (all generated content)
# IMPORTANT: ignore contents, not the directory, so exceptions work
# -----------------------------------------------------------------------------
outputs/**
!outputs/**/
!outputs/README.md
!outputs/**/.gitkeep

# -----------------------------------------------------------------------------
# Archive organization (ignore contents but keep README/.gitkeep)
# IMPORTANT: ignore contents, not the directory, so exceptions work
# -----------------------------------------------------------------------------
archive/experiments/**
archive/deprecated/**
archive/legacy/**
archive/*.jpg
archive/*.jpeg
archive/*.png
!archive/README.md
!archive/**/
!archive/**/.gitkeep

# -----------------------------------------------------------------------------
# Projects directory (client-specific production files - not for version control)
# Ignore contents but keep directory structure
# -----------------------------------------------------------------------------
projects/*
!projects/.gitkeep

# -----------------------------------------------------------------------------
# Client construction documents and architectural PDFs (sensitive)
# -----------------------------------------------------------------------------
Architectural_Plans/**/*.pdf

external/
.cas/
tmp_tensor.npy

# Local archive reports (reproducible)
archive_reports/

# Local scratch/artifacts
local_artifacts/
*.numbers
# secure-landing isolated Next build outputs
web/secure-landing/.next-build-verify/
web/secure-landing/.next-smoke-*/

# uv lockfile — auto-generated on every uv run; not tracked
uv.lock
