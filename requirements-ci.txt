# CI environment: runtime + test deps
# Uses base (core) dependencies only for faster CI builds.
# Heavy benchmarks/stress tests should be marker-filtered and run manually or in a dedicated workflow.
-r requirements.txt

# Testing / coverage / property-based tests
pytest>=8,<10
pytest-cov>=4.0,<8
hypothesis>=6,<7

# Required for RAG system and workflow tests
PyYAML>=6.0,<7

# Required for depth_writer (used in core pipeline tests)
opencv-python>=4.8.0,<5
