# Transformation Portal - CI Requirements
#
# This file points to the new layered dependency management system.
# For detailed documentation, see requirements/README.md
#
# For CI/CD pipelines, install from pinned files:
#   pip install -r requirements/base.txt -r requirements/ci.txt
#
# Or for full CI environment:
#   pip install -r requirements/all.txt

# Include pinned base and CI requirements
-r requirements/base.txt
-r requirements/ci.txt
