# ingest batch fixtures

These are minimal synthetic fixtures for ingest batch determinism tests.

- The files are intentionally tiny and not camera-valid media assets.
- Batch determinism tests use a deterministic fake ingest payload factory.
- Fixture value is path/content diversity across mixed extensions (`.dng`, `.jpg`, `.mov`) with stable bytes.
