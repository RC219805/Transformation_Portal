# Transformation Portal - Master Requirements File
# 
# This file points to the new layered dependency management system.
# For detailed documentation, see requirements/README.md
#
# The requirements are now organized into layers:
#   - base: Core runtime dependencies
#   - ml: Optional ML and deep learning packages
#   - dev: Development and testing tools
#   - ci: CI/CD pipeline tools
#   - all: Everything combined
#
# For reproducible builds, install from the pinned .txt files:
#   pip install -r requirements/all.txt
#
# For the latest allowed versions, use package extras:
#   pip install -e ".[all]"

# Include all pinned requirements
-r requirements/all.txt
