# Transformation Portal - Core Runtime Dependencies
# ==================================================
# This is the root aggregate requirements file for core runtime dependencies.
# Local repository setup should use the Make targets below so the pinned
# dependency lanes, editable package install, and pip check stay in sync.
#
# Installation options:
#   make install-core                         # Repo-managed core .venv (recommended)
#   make install-ml-core                      # Supported target-owned ML core install
#   make repair-core-venv                     # Recreate and validate the core .venv
#
# For CI/CD environments, use requirements-ci.txt
# For development, use requirements-dev.txt

# Core runtime essentials (base package)
-r requirements/base.txt

# Optional ML dependencies are intentionally target-owned.
# Do not add removed umbrella locks such as requirements/ml.txt here.
# Use `make install-ml-core` or see requirements/README.md for supported flows.

# Installation notes:
# 1. Base install (core features): ~500MB
#    - Image processing, batch operations, basic color grading
#    - No AI/ML features (depth estimation, upscaling, etc.)
#
# 2. Supported ML core install:
#    - Uses the checked-in target-owned Apple Silicon ML lock when available
#    - Enables governed depth/ML workflows on supported runtimes
#    - To enable: run `make install-ml-core` or see requirements/README.md
#      for target-owned flows; pyproject.toml extras are metadata pointers,
#      not the governed operator install surface
#
# 3. For development: use requirements-dev.txt or requirements/all.txt
#
# See requirements/README.md for detailed documentation on the layered system.
