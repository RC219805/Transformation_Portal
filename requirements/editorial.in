# Optional editorial runtime, native Darwin arm64 / CPython 3.12 only.
# Core package versions remain constrained by the governed generic lock.
# Regenerate with scripts/setup/editorial_runtime.py lock using the pinned compiler.
-c all.txt
numpy>=1.24,<2.5.0
Pillow>=10.3.0,<13
scipy>=1.15,<2
opencv-python>=4.8.0,<5
tifffile>=2023.7.18,<2027
imagecodecs>=2023.9.18,<2027
PyYAML>=6.0,<7
tqdm>=4.66,<5
rawpy==0.27.0             # Reviewed LibRaw decoder and deterministic native wheel closure
reportlab==4.5.1         # Reviewed contact-sheet renderer and deterministic PDF runtime
ExifRead==3.5.1          # Reviewed measured RAW capture metadata reader
piexif==1.1.3            # Reviewed lossless JPEG EXIF insertion and ICC preservation
pip==26.2.1              # Governed hash-verified runtime bootstrap, matching compiler pip
