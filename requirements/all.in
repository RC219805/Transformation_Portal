# Aggregate of the checked-in dependency contract
# Optional ML layers are excluded because they do not have trusted checked-in lockfiles.
# Run `make compile` in this directory to generate pinned all.txt

-r base.in
-r dev.in
-r ci.in

# Keep repository lock/install environments compatible with the governed compiler.
click>=8.4.2,<8.5
