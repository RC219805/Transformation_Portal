# Optional Apple CoreML acceleration extras
# macOS-only due to platform requirements.
# No checked-in standalone CoreML lock is generated from this file. Use the
# supported Apple Silicon ML core lock when it already includes CoreML tooling.
#
# WARNING:
# - coremltools is only available on macOS
# - This layer is automatically skipped on non-Darwin platforms in CI
# - Use environment markers if compiling cross-platform lockfiles
#
# Capability contract:
# - This is an OPTIONAL layer. Absence uses standard PyTorch backends.
# - `make install-ml-coreml` fails closed unless a trusted CoreML lock exists.
# - Do not install from a missing ml-coreml.txt host lock artifact.

# Apple Silicon acceleration and CoreML conversion tools
coremltools>=7.0 ; platform_system == "Darwin"
