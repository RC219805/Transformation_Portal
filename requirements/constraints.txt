# This constraints file is used to steer the dependency resolver away from known
# incompatible or vulnerable transitive upgrades.
#
# Usage:
#   - Pass this file to pip-compile with the --constraint option to restrict
#     transitive dependencies:
#       pip-compile --constraint requirements/constraints.txt requirements.in
#   - Alternatively, use pip install --constraint requirements/constraints.txt <package>
#
# Scope:
#   - These constraints apply to transitive dependencies only (not direct requirements).
#   - They are intentionally conservative to avoid problematic upgrades.
#
# Maintenance:
#   - Review and update these pins when upgrading direct dependencies, after security
#     advisories, or before major releases.
#   - Update pins via a locked requirements file and after testing for compatibility.

# ML dependency constraints to prevent resolver conflicts
controlnet-aux<0.0.9

# Security Advisory: CVE-2024-27763
# BasicSR <= 1.4.2 has a command injection vulnerability via SLURM_NODELIST.
# This constraint uses an impossible version specifier to exclude basicsr from being resolved.
basicsr>=999.0.0
