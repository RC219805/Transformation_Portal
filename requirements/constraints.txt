# Dependency Constraints
# Blocks installation of banned/problematic packages
# Governed installer baseline: pip==26.2.1; --build-constraint requires pip>=25.3.
# Use with: pip install -c requirements/constraints.txt --build-constraint requirements/constraints.txt

# Security/quality bans - these packages are NOT allowed

# realesrgan  # Unmaintained, use local implementation (HARD-BLOCKED)
realesrgan>=9999.0.0
# gfpgan  # Unstable dependencies, use alternative (HARD-BLOCKED)
gfpgan>=9999.0.0

# basicsr  # CVE-2024-27763: command injection via SLURM_NODELIST (HARD-BLOCKED)
basicsr>=9999.0.0

# Spatial AI Foundation: Supply chain risks pending audit
# apache-iceberg  # Java interop (py4j), supply chain risk not assessed (ADR-024 pending)
apache-iceberg>=9999.0.0
# pyiceberg  # Wrapper for apache-iceberg, same risk profile
pyiceberg>=9999.0.0
