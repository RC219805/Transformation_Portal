# Developer and testing tools
# These are not needed in production environments.
# This file contains abstract requirements with version ranges.
# Run `make compile` in this directory to generate pinned dev.txt

# Testing frameworks
pytest>=8.0,<10
pytest-cov>=4.0,<8
pytest-asyncio>=1.3.0,<2  # async test support for ExecutionManager/async tests; 1.3.0+ required for pytest 9
pytest-json-report>=1.5,<2  # JSON test result reporting for flake tracking
pytest-rerunfailures>=14.0,<17  # Rerun flaky tests automatically
pytest-xdist>=3.5,<4  # parallel test execution (ADR-044 CI optimization)
diff-cover>=10.2.1,<11  # diff coverage for PR validation (Phase 0 coverage infrastructure)
httpx>=0.28,<1  # required for FastAPI/Starlette TestClient in HTTP contract tests
hypothesis>=6.0,<7
jsonschema>=4.21.0,<5  # required for machine-mode contract schema validation tests
cryptography==47.0.0  # CAS determinism + Security: CVE-2024-0727 / GHSA-9v9h-cgj8-h64p fixed
moto[s3]>=5.0,<6  # S3 mock for the Phase 4.A S3ArtifactStore contract test branch

# Type checking and linting
mypy>=1.10
flake8>=7.0
pylint>=3.0
black==26.3.1  # CAS determinism + Security: arbitrary file writes fixed in 26.3.1
isort>=5.13,<9
autopep8>=2.0,<3
libcst>=1.2,<2

# Additional development utilities
types-PyYAML>=6.0.12
pre-commit>=4.0,<5  # required by `make install-hooks` and `make pre-commit`; gated by .pre-commit-config.yaml
